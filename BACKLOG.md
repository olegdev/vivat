# Backlog

Only things that are actually open. Delete lines as they land.

## Known gaps

- Product cards show the same image three times — the gallery and its dots are
  real, the data isn't. Needs per-product image sets.
- Card data across all sections is mock (`src/pages/customer/main.js`). Counts
  were padded to ten to match the mobile 2×5 layout; prices and titles are
  invented.
- Hero slides 2 and 3 are mock — only slide 1 exists in Figma.
- Yandex Maps key is inlined as a fallback in `main.js`. Move to
  `VITE_YANDEX_MAPS_KEY` before this is public.
- PDP's «Состав» tab has no Figma frame — `partials/pdp-specs.html` holds
  placeholder rows there. Replace when the design lands.

## Навигация — что осталось на `#` и почему

Разводка сделана 01.09.2026 по `docs/LINK-MAP.md`; там же решения клиента по
каждому вопросу. Здесь только открытое и намеренные расхождения с макетом.

- **«О компании» и «О нас» открывают ролик, 03.09.2026.** Адрес нашёлся на
  живом сайте клиента (mebel.com/contacts) — RuTube, не YouTube, как
  предполагалось раньше: `partials/modal-about.html`, модалка без формы на
  общем modal-shell (нет фрейма в макете).
- **Соцсети, «Политика конфиденциальности» и «Построить маршрут» остаются
  `#` по решению клиента** — адресов нет и страницы политики тоже. Соцсетей
  шесть в четырёх местах (блок «Подпишитесь на нас», подвал ×2, бургер),
  политика — в подвале ×2, в трёх модалках и в согласии на заказе.
- **Кабинета нет ни одной страницей**, поэтому «Личный кабинет», «Мой
  кабинет», «Выход», «Кабинет» в тапбаре и иконка профиля в шапке никуда не
  ведут. «Конструктор» — тоже: страницы нет ни у нас, ни в макете.
- **«Компания» и «Полезная информация» в бургере остались `#`.** Это заголовки
  веток подвала, а не страницы; ответа, куда они должны вести из меню, нет.
- **Страницы отдельной новости нет.** «Подробнее» в оповещении на обеих
  главных ведёт на листинг с якорем — `news.html#news-1`, — а не на новость;
  карточки новостей на дилерской главной ведут на листинг без якоря. Якорь
  строится как `news-<номер>` в `pages/dealer/news.js`; в Blade это
  идентификатор записи.
- **«Подробнее» у семнадцати новостей снято по решению клиента** (01.09.2026):
  ссылка вела в никуда, осталась только та, что скачивает файл, — у двух
  новостей. Расхождение с кадром намеренное, `npm run audit` его покажет.
- **Параметр фильтра — подпись, а не идентификатор.** `catalog.html?category=
  Кухни`, `?collection=Фьюжн`, `?filter=Белые`, `?modules=1`. Своего словаря
  слагов макет не даёт, выдумывать его нельзя; в Blade подставится то, чем
  рубрика зовётся на бэке — меняется одна функция `catalogHref()` в
  `components/links.js`.
- **«Все модули Фьюжн» ведёт в `catalog.html?modules=1`.** Отдельной выдачи
  модулей в макете нет, а переключатель «Только модули» есть лишь у дилерского
  каталога — на покупательском параметр пока ничего не включает.
- **Выбор города в самовывозе получил выпадашку, которой нет в кадре.** Макет
  рисует «Ваш регион: Москва» ссылкой (`text-action size=s` 1209:95266) и
  прототипа на ней не даёт. Подключено к общему выбору города; чтобы это работало
  на 1440, рядом с триггером понадобилась та же панель, что у «Москвы» в
  шапке. Ниже md оба открывают общий лист. Показать дизайнеру.
- **Контентные страницы b2b лежат под дилерской обвязкой, а ссылаются на них
  из покупательского подвала.** Так и задумано: страницы общие, а полоска и
  ветки подвала зависят от того, залогинен ли дилер (клиент, 01.09.2026). В
  прототипе это `data-user` в разметке, в Blade придёт из сессии.

## Дилерский раздел — ждём ответа дизайнера

Полная карта b2b-секций и все вопросы: `docs/FIGMA-MAP.md`. Здесь только то,
что уже уперлось в вёрстку.

- **Дилерская главная на 360 собрана без своего фрейма.** 360-фрейма у
  882:107882 по-прежнему нет. Шапка, нижняя навигация и выбор прайс-листа взяты
  из дилерского каталога 2225:160540 — это макет. А вот **новости рельсом,
  бургер-меню и дилерские ветки мобильного подвала** не нарисованы нигде и
  выведены нами: новости — 320-е карточки, как у мобильных «Акций», и две
  кнопки друг под другом (в ряд не влезают); меню — навигация дилерской шапки
  на 1440 («Каталог», «Конструктор», «Контакты») плюс ссылки дилерской полоски
  («Для бизнеса», «Мой кабинет», «Выход»); подвал — те же две ветки, что у
  десктопного. Показать дизайнеру.
- **Из дилерского бургер-меню убраны «Где купить», «Компания» и «Полезная
  информация».** Они достались от покупательского набора (1997:255059), а в
  дилерской навигации их нет: шапка на 1440 несёт «Конструктор» и «Контакты».
  При этом «Полезная информация» и «Компания» — реальные ветки дилерского
  подвала, так что вопрос открыт: должны ли они быть и в меню.
- **На 360 дилерские фреймы подставляют покупательский подвал.** В каталоге
  2225:160540 инстанс подвала — вариант `user=Default` (1821:327818), тогда как
  на дилерском 1440 стоит `user=Dealer` (380:49273). Мы рендерим дилерский на
  обеих ширинах: иначе мобильная версия теряет «Личный кабинет» и вторую ветку
  ссылок, которые на десктопе есть. Похоже на тот же пропуск, что и с крошками —
  360-фреймы дилера собраны из покупательских частей.
- **Дилерский каталог на 360 идёт без хлебных крошек и заголовка.** Во фрейме
  2225:160540 за шапкой сразу следует панель настроек: ни `breadcrumbs`, ни
  `title-block` там нет, хотя на дилерском 1440 (953:121911) и на
  покупательском 360 (1997:267656) они есть. Собрано по макету — ниже `md`
  блок скрыт. Спросить дизайнера, это решение или пропуск.
- **«Только модули» — тумблер без прототипа.** Дилерская панель настроек
  (`catalog-settings type=dealer`, 882:89379 на 1440 и 1806:238263 на 360)
  добавляет к покупательской один тумблер, и что он делает, макет не говорит.
  По решению заказчика это обычный фильтр: применяется сразу и уходит в URL
  полем `modules_only`. Серверного поля у товара нет — в фикстурах стоит
  заглушка `isModule` через один (`data/catalog.js`). Нужно поле или правило.
- **Дилерская панель фильтров на 1440 — отцепленная копия.** 953:121956 рисует
  пять групп вместо шести (нет «Формы кухни»), и у первой потерян заголовок —
  рендерится текст мастера «Форма кухни» вместо «Коллекции». Покупательский
  фрейм 759:69866 держит ту же панель инстансом компонента, поэтому
  переиспользуем его. Поправить в исходнике.
- **Инстанс дилерской панели настроек не несёт ни одного переопределения
  текста** (то же и у покупательской 882:89607), поэтому `npm run audit` по
  ней сравнивает страницу с филлером мастера «Текст» и всегда даёт расхождение.
  Проверять эту панель приходится по размерам боксов (`fig.mjs inst`), а не по
  копии. Дизайнеру: проставить тексты в инстансах.
- **Откуда берётся «Рекомендованная цена».** Пересчёт работает, но множителя в
  дизайне нет: в `data/dealer-home.js` стоит заглушка `RRP_FACTOR = 2`. Нужно
  поле у товара или правило.
- **Раскрытая «Своя наценка» на 1440 не нарисована.** Вариант
  `dropdown-header desktop/open` (1299:49518) рисует три строки по 44 и всё.
  Поле ввода и «Применить» перенесены с мобильной шторки 2225:164865.
- **Опечатка в макете: «Рекомендованая цена»** (одна «н») — рендерим
  «Рекомендованная». Поправить в исходнике, тогда `npm run audit` по строкам
  списка снова сойдётся.
- **Подпись «Минимальная наценка 50%»** у дизайнера 11/14 — размера, которого
  нет в шкале. Взят ближайший токен `body-xs` (12/16).
- **Отступ полоски.** Инстанс говорит `padH=12`, мастер — 24, дочерний фрейм
  стоит на x=24. Взято 24: при 12 тумблер залезает в скругление 32px.
- **Тумблер «Показывать цену»** прототипа не имеет; поведение задал заказчик:
  это выключатель применения выбранного прайс-листа. Выключен — цены оптовые,
  включён — выбранный в списке режим. Ничего не скрывает. Открытым остаётся
  одно: выбор режима при выключенном тумблере сейчас **не** включает его
  автоматически — цены не меняются, пока тумблер не вернут. Подтвердить.
- **Дилерский PDP на 360 собран без своего фрейма.** У 953:122180 и обеих его
  панелей (1686:59341, 1686:59383) 360-фреймов нет. Тело ниже `md` взято с
  покупательского мобильного PDP (1997:305719 и его панели), обвязка — из
  дилерского каталога 2225:160540. Выведено нами три вещи: **мобильная сводка в
  дилерском виде** (варианта `device=mobile, Customer=b2b, Type=PDP` не
  существует — перенесли на неё десктопную дельту b2b: цена одной строкой, без
  «от», без старой цены, без бейджа скидки и без строки notice), **подпись
  мобильной кнопки заказа** («Оформить • 43 335₽» вместо «Оформить • от 43 335₽»)
  и **серая полоса под отзывами** на 360. Показать дизайнеру.
- **Рельс «Вся коллекция Фьюжн» на дилерском PDP нарисован с «от».** Во фрейме
  1686:63229 карточки идут как «от 450 010₽» — покупательский инстанс,
  скопированный вместе со страницей. При этом дилерский каталог (1686:158738) и
  сводка этого же PDP «от» не имеют. По решению заказчика от 12.08.2026 рендерим
  без «от» на всей дилерской половине. Поправить в исходнике.
- **Нижняя панель с ценой у дилера не нарисована.** У покупателя это отдельные
  фреймы `sticky price` 1884:366246 и `DPD` 2027:83602; у дилерского PDP их нет.
  Переиспользованы покупательские (`partials/sticky-price.html`) — без них на
  360 у страницы вообще не было бы кнопки заказа, мобильная сводка её не несёт.
  Цена в них приведена к дилерскому виду: без «от» и без старой цены.
- **Пересчёт цены товара прайс-листом прототипа не имеет.** На дилерском PDP
  цена в сводке и в нижней панели теперь подчиняется выбранному прайс-листу так
  же, как цены карточек (решение заказчика от 12.08.2026). В макете связи между
  полоской и ценой товара не нарисовано ни на одном фрейме.
- **Соцсети в подвале**: на десктопе шесть (с MAX), на мобильном пять.
  Расхождение досталось от покупательских фреймов, у дилерских своего 360 нет.
- **Блок «Производство»**: у дилерского фрейма затемнение `#111111/0.8`, у нас
  `overlay-middle` (0.6) — как было на покупательской. Уточнить, какое верно.
- **Полоса alert** на покупательской главной — старая инлайновая копия без
  иконки и крестика, предшествует `partials/alert.html`. Свести к партиалу,
  когда дизайнер подтвердит, что иконка и × нужны и там.

## Seams to wire (form + request seam pattern — see SOLUTIONS.md)

Places that will be a server round-trip in the Blade build. Done so far: catalog
filter drawer, popular-carousel tabs, site search (overlay + suggest),
add-to-cart, stores filter.

The JS-string structure debt is unwound: every repeated unit is now a clean HTML
`<template>` in its partial that the component clones (product-card, catalog-menu,
mobile-menu, stores). What stays in JS is only the mock *data* arrays
(`categories`, `stores`, the product lists) — in Blade those come from the model
and are printed into the same templates; nothing structural remains to convert.

- Search suggest is a seam in one function, `searchSuggest()` in
  components/search.js — it returns `{ hints, chips, items }`, exactly the shape
  a `/search/suggest?q=…` response should have. Its mock corpus is
  `src/data/search.js`; both go away when the endpoint lands. The overlay's own
  `<form action="/search" name="q">` already submits the real query.
- The designer re-cut `cards-other size=s` (Figma 632:27760): it now carries a
  swatch + comments row above the category tag, 322×410 instead of 386. The
  search card (`data-pcard-search`) is built to the new shape; the PDP's
  "Добавьте в корзину" rail still uses the old `data-pcard-other-s` template. No
  visible difference today — that rail's fixtures carry no swatches — but the
  two templates should be folded into one when someone next touches the PDP.

## Планшет и широкий экран — вопросы по макету

- **На странице модуля есть цена, отзывы, «Где купить» и бар с ценой, которых
  в макете нет.** Решение клиента (01.09.2026): якорный бар несёт «Отзывы» и
  «Где купить», а сами разделы в макете скрыты (2488:127174), поэтому блоки
  взяты с кухонной PDP; цена и `sticky-price` добавлены туда же, хотя сводка
  модуля (2488:130058) их не рисует — там только «Получить оптовую цену».
  Показать дизайнеру.

- **Куда ведут карточки «Обучающих материалов», макет не говорит.** Восемь
  превью с подписями (2507:138626 и его 768/360-близнецы), но ни ссылки, ни
  оверлея с плеером во фреймах нет — карточки сейчас ведут в «#». Спросить,
  открывается ли видео на странице, в модалке или уводит на внешний сервис.

- **В планшетном фрейме заказа есть крошки и зелёная полоса, которых нет на
  1440.** Фрейм 2488:138909 (768) несёт `breadcrumbs` 768x56 и
  `Frame 2087325443` 768x828, а его же страница на 1440 (2488:138018) — ни
  того ни другого, как и наша. Фрейм при этом назван «PDP», и оба блока — ровно
  то, что есть у PDP, откуда его, судя по всему, и скопировали. Не добавлял;
  спросить, нужны ли они на заказе.

- **«Итого» в новой комплектации не сходится с собственными строками.** Во
  фрейме 2567:158700 веса 27.064, 19.009, 17.032, 22.064 и 46 при количествах
  1, 1, 2, 1, 1 — сумма 148.201 кг, а написано 145.201; с объёмами то же:
  0.25163 против написанных 0.24334. Количество (6) при этом сходится. Мы
  считаем итог из строк, а не храним его: на живой странице сумму даёт сервер,
  и второй источник правды тут завести нельзя. Показать дизайнеру — цифры в
  макете править ему.
- **У состояния «в корзине» не нарисовано, как из него выйти.** Секция
  `active cards` 2462:212312 даёт только сам вид: круг #414141 с белым
  `order-ok` на десктопе и тёмная пилюля «в корзине» на узких карточках.
  Ни минуса, ни степпера, ни повторного клика в макете нет. Сейчас повторный
  клик добавляет ещё одну штуку, а кнопка остаётся в этом состоянии — убрать
  товар можно только со страницы заказа. Спросить дизайнера.
- **Варианта `condition=active` без hover в наборе icon-button нет** — есть
  только `active-hover` (881:51240). Взят он: это то же соглашение, что у
  пунктов `menu-b2b`, где текущий пункт помечен вариантом hover.

- **Сверка высот главной вскрыла старый дрейф десктопа.** Проверка блок за
  блоком (высота каждого прямого потомка оболочки против фрейма) показала, что
  расхождения одинаковы на 768 и на 1440, то есть к адаптиву отношения не имеют.
  После починки хвостового отступа в шапке секции (`chipsSlot.remove()`
  выбрасывал 24 у каждой секции без чипсов) осталось:

  | блок | Δ на 1440 | Δ на 768 | причина |
  |---|---|---|---|
  | Action (промо) | −32 | −32 | плитка `news-card` 635:5432 рисуется 485 против 517 |
  | «Акции», «Популярные» | +31 / +18 | +29 / +39 | карточка `cards-other` 632:27846: у нас 489 против 457 |
  | подвал | +65 | +65 | колонки подвала выше макета на обеих ширинах |
  | «Модульные» | −5 | −5 | округление высот строк в карточке |

  Это отдельная задача по десктопу; в адаптив её не смешивать.

- **Блоки, совпавшие с планшетным фреймом точно:** шапка (±1 из-за линии),
  алерт, баннер, оба `spacing`, категории, соцсети, карта, «Производство».

*Разбор целиком: `docs/superpowers/specs/2026-08-26-responsive-tablet-wide-design.md`.*

- **Полноэкранная высота работает только до 1900 (26.08.2026).** Выше ширина
  уже упёрлась в ограничитель, и растущая высота при неподвижном кадре читается
  как ошибка, — поэтому герой и «Производство» возвращаются там к нарисованным
  640 и 842. Диапазон закрыт прямо в варианте `wide`. Помечено клиентом как
  временное; на границе 1440 высота по-прежнему прыгает с 640 на 100vh.
- **Полноэкранность по ширине отменена (26.08.2026).** Комментарий дизайнера
  на широкой главной — «этот блок с шапкой занимает 100% высоты и ширины
  экрана», и то же про «Производство». Ширину клиент отменил: шапка, алерт,
  герой и «Производство» упираются в 1900, как остальные блоки, и центрируются.
  Решение помечено как временное — клиент показывает его дизайнеру. Высота
  `100vh` выше 1440 при этом осталась.
- **Порог полноэкранного героя — 1440, решение клиента (26.08.2026).** Выше него
  шапка+алерт+баннер и «Производство» занимают весь экран. Побочный эффект: на
  1440×900 герой становится 744 вместо нарисованных 640, то есть ровно на
  границе макет и код расходятся. Дизайнеру показать.
- **На планшете шесть плиток категорий вместо семи.** Ряды 3+3 (2477:114199 /
  2477:114221) против 3+4 на 1440. Уходит седьмая — белая плитка-ссылка
  («56 модулей →»), единственная с заливкой `#ffffff`. В 768-фрейме её просто
  нет; построено по фрейму. Спросить, намеренно ли — на планшете это единственная
  точка входа в раздел, которая пропадает.
- **`Order 1440x2841` (2488:138018) лежит в планшетной секции.** Планшетный
  заказ — это 2488:138909, который назван `PDP` (подтверждено клиентом). Что
  тогда за фрейм 2488:138018 и не забыт ли он там — вопрос дизайнеру.

## Хвосты

- **Нумерация шагов заказа расходится между поколениями кадров.** Старые
  `Order-step1/step2` (953:120324, 953:121003) читают «Шаг 2 из 3» и «Шаг 3 из
  3», новые (2241:171043, 2396:152391) — «Шаг 1 из 2» и «Шаг 2 из 2». Выведено
  по новым: корзина шагом не считается. Подтвердить.
- **«Ваши» есть на 360 и нет на 1440.** Подзаголовок шага 2 покупательского
  заказа во фрейме 2082:145505 читается «Пожалуйста, укажите **ваши**
  контактные данные для связи!», а на 1440 (953:115326) — без «ваши». Выведена
  мобильная редакция на обеих ширинах: фрейм 360 новее. `npm run audit` на
  десктопном инстансе покажет расхождение — оно известное.
- **Список городов сделан; список салонов по городам — нет.** Города взяты из
  самого макета: `left-side type=city` 1859:335134 при выбранной Москве рисует
  ровно четыре строки — Санкт-Петербург, Нижний Новгород, Ростов-на-Дону,
  Рязань, — то есть текущий город в списке не повторяется, а сам список
  совпадает с оптовым селектором Контактов. Живёт в `data/cities.js`.
  Что остаётся: выбор города меняет подпись везде и дёргает сеть `setCity()`,
  но **список салонов не фильтруется** — в `data/stores.js` у точек нет города,
  там десять адресов по Москве. Появятся салоны по городам — фильтр встаёт в
  `city:change`.
- **Страница модуля: два вопроса по фрейму.** Построена по 2488:127146 / 136015
  / 128722. Открытое: (1) якорный бар несёт «Отзывы» и «Где купить», а самих
  разделов на странице нет — блок с ними в макете скрыт (2488:127174
  ⃠hidden-in-master), так что две ссылки ведут в никуда; построено по фрейму.
  (2) таблица характеристик своей копии не получила: инстанс 2488:127165
  переопределяет три ячейки из двадцати, остальное — филлер кухонной таблицы, и
  она же взята. Обе вещи показать дизайнеру.
- **Широкого PDP в макете нет.** Сделано по аналогии с каталогом: колонка фото
  тянется (`flex-1`), сохраняя пропорцию кадра 900/671, сводка остаётся 412, а
  блок характеристик — 940, как нарисован на 1440. На 1900 это даёт кадр
  1360x1014. Показать дизайнеру: возможно, колонку фото не надо растягивать
  вовсе.
- **Ящика фильтров на планшете дизайнер не рисовал.** В планшетной секции есть
  только `Catalog-default` 2483:202337; состояний с открытыми фильтрами там нет
  (на 1440 это Catalog-all-filters 759:69866, на 360 — свой полноэкранный).
  На 768 сейчас работает десктопный ящик — панель слева над затемнением. Раз
  именованные пилюли на планшете схлопнуты в воронку, ящик там единственный
  вход в фильтры, и его стоит показать дизайнеру.
- **В планшетном меню на строку меньше, чем в мобильном.** Фрейм `Menu step 1`
  (2483:192772) показывает пять строк — Каталог, Где купить, Компания, Для
  бизнеса, Стать дилером, — а «Полезная информация» лежит там скрытой и шириной
  328, то есть осталась от мобильного мастера. На 360 она есть. Оставил все
  шесть: убирать пункт меню по скрытому слою рискованно. Спросить.
- **Коралловая подложка блока салонов на 360 не нарисована.** На 1440 и 768
  `map-general` залит `#f8bb92`, а мобильная секция (`section / Наши салоны`
  2395:106203, она же 1968:71568) не несёт заливки вовсе — ни сама, ни её
  родитель, ни рамка карты. Построено по фрейму: на мобиле блок белый. Клиент
  ждал коралловый — спросить дизайнера, намеренный ли это разрыв.
- **У листа выбора города своего фрейма нет.** Дизайнер рисует это состояние
  внутри панели «Где купить»: список подменяет тело панели, а в шапке появляется
  стрелка назад. Поверхности для «Москвы» из шапки сайта в макете нет вовсе —
  выпадашка на десктопе и лист на мобиле это решение клиента (26.08.2026).
  Лист собран на общей оболочке `.modal-scrim` / `.modal-panel` и повторяет
  нарисованную панель: ручка, ряд 40 со стрелкой у левого края и городом у
  правого, линия 1px #e7e7e7 под шапкой, список с полями 10/24 и зазором 8.
  Заголовка ему не сочиняли — в шапке стоит текущий город с пином (1859:335085).
  Была попытка вести «Москву» из шапки в полноэкранную карту — клиент отменил:
  клик просто показывает выбор города.

## Дилерский заказ — вопросы по макету

- **Список способов оплаты не нарисован нигде.** Селект «Оплата» несёт только
  плейсхолдер, и мобильной шторки под него нет ни в одном фрейме. Собран
  рабочий `<select>` с одним пунктом; варианты нужны от заказчика.
- **Надбавки за расстояние и этаж — без правила.** Оба бейджа во фрейме
  1415:67654 читают «+222₽», хотя поля разные (12 км и 8 этаж), а стоимость
  доставки — константа 3414₽. Ничего не считаем: цифры лежат в
  `data/dealer-order.js`, пересчёт — шов `applyDelivery()`. Нужна формула.
- **«Удалить» в шапке корзины дилерский инстанс не переопределяет** — рендерит
  мастерскую заглушку «56 моделей ✕». Взято «Удалить», как в покупательском
  фрейме 943:34134. Подтвердить.
- **Два мобильных фрейма расходятся по кнопке в сводке.** В 2225:202954 у
  сводки есть блок кнопок, в полном 2225:167283 его нет. Собрано по полному:
  сводка без кнопок, CTA — всегда видимая липкая полоса. Проверить.
- **«Не выбрано» против «Не выбрана».** 1209:95219 рисует «Не выбрано»,
  1415:67609 — «Не выбрана» в том же селекте. Взято «Не выбрано» — по основному
  фрейму; в сводке — «не выбран» («Способ оплаты»), как во фрейме. Три формы
  одного значения в одном экране; нужна одна.
- ~~**У маленького степпера нет варианта «мусорка».**~~ Закрыто 02.09.2026:
  дизайнер заполнил клетку `size=s, count=1` (2029:129546), и мобильная
  карточка заказа 2084:145513 берёт именно её. Мусорка теперь на обеих
  ширинах, строка с телефона удаляется.
- **«Итого» есть на 360 и нет на 1440.** 953:149252 такой строки не рисует,
  2029:158160 рисует. Собрано по фреймам; похоже на пропуск в десктопном.
- **Кнопка липкой полосы на 360 нарисована с «от»** («Сформировать заказ • от
  43 335₽») — копия покупательского инстанса. Выведено без «от», как все
  дилерские цены (то же решение, что на дилерском PDP).
- **Экрана подтверждения в дилерской секции нет** — берём покупательский
  оверлей 2241:158412 / 2241:158297 (мобильный дилерский 2238:157458 — он же).

## Not started

Покупательские страницы (главная, каталог, акции, PDP, заказ) и дилерские
(главная, каталог, PDP, заказ) свёрстаны. Осталось — карта и node-id в
`docs/FIGMA-MAP.md`:

- **Секция `B2b additional`** — контентные страницы. Свёрстаны Доставка и
  Контакты, а также четыре модальных окна той же секции (см. ниже). Остальные
  девять ждут; оболочка, меню раздела, его мобильная шторка и аккордеон уже
  общие, так что каждая следующая страница — это только содержимое колонки.

## Контентные страницы b2b — вопросы по макету

- **Пять пунктов меню раздела ведут в никуда.** Страниц нет ни в макете, ни у
  нас: Поставщикам, Прайс-листы, Каталог мебели, Обучающие материалы,
  Методические пособия. Стоят с `href="#"`. Нужен ответ, рисовать ли их или
  убирать из меню.
- **«Каталог декоров» нарисован дважды** — 1488:127306 (1440×3807) и
  1488:69674 (1440×1675), у обоих один H1. Что означает второе состояние,
  непонятно: свёрнутый список? пустая выдача? Нужен ответ.
- **Заголовок первого блока меню не переопределён** и берётся из мастера —
  «Помощь». Проверить, что это и задумано, а не забытый оверрайд.
- **Страница называется по-разному в меню и в крошках:** «Адреса» в
  `menu-b2b`, «Контакты» в заголовке страницы. Воспроизведено как есть.
- **Ответ написан только у одного вопроса FAQ на Доставке** — у того, который
  нарисован раскрытым. У остальных четырёх текста нет ни в инстансе, ни в
  мастере; слоты пустые. Нужны ответы.
- **Поле «Найти в документах» в мобильной шторке меню** нарисовано, но что оно
  ищет — не сказано, прототипа на нём нет. Отрисовано без поведения.
- **Нижней навигации на мобильных контентных фреймах нет** ни на одном, хотя
  дилерский каталог на 360 её несёт. Добавили дилерскую — решение заказчика,
  не макета.
- **Опечатки макета, исправленные у нас** (`npm run audit` покажет их как
  известные расхождения): «Каталог де**к**торов» → «Каталог декоров»; хвостовые
  `\r` в «Обучающие материалы», «Каталог декоров», «Раков Евгений»,
  «Шапошников Александр», «Чепаева Светлана», «Оптовый отдел», «Оптовый склад
  VIVAT» и в подписи врезки на Доставке.
- **Описание блоков «Наши сотрудники» и «Наш склад» на Контактах одинаковое** —
  «Свяжитесь с профильным специалистом и мы поможем подобрать решение.»
  Дизайнер переопределил его в обоих инстансах одним текстом. Воспроизведено;
  похоже на копипаст.
- **Кнопка «В раздел» в заголовочном блоке не отрисовывается.** `inst` находит
  её (134×44) во всех контентных фреймах, но на рендере фрейм `buttons` пуст —
  как у заголовка каталога. Не рисуем.

## Модальные окна — вопросы по макету

- **Экрана «отправлено» нет ни у одного из четырёх окон.** В секции нарисованы
  только сами формы. Все четыре после отправки просто закрываются; шов на окно
  в `components/modals.js`. Нужен ответ, показывать ли подтверждение.
- **Переход на дилерскую главную после входа — наше решение.** Макет не говорит
  ничего: дилерский режим у нас это отдельные файлы в `pages/dealer/`, а не
  сессия, поэтому `submitDealerLogin` уводит на `dealer/main.html`, иначе форма
  входа никуда не ведёт.
- **В окне директора подписи и плейсхолдеры переставлены.** Под «Телефон» стоит
  «Адрес электронной почты», под «E-mail» — «Номер телефона» (1534:70720).
  Сверстано по смыслу; в макете поправить.
- **Заголовок в мобильной шапке у трёх окон из четырёх** читается «Войти в
  режим дилера» (2209:216418, 2225:96983, 2225:97794) — копипаста инстанса.
  Шапка в рендере рисует только крестик, поэтому заголовок взят из тела.
- **Серая кнопка «Отмена» в мастере есть, в инстансах скрыта.** Во всех четырёх
  окнах `modal-button-container` отдаёт одну кнопку во всю ширину. Если вторая
  всё-таки нужна — сказать.

- **Адрес склада на 1440 и 360 разный.** Десктопная панель (1456:60503) рисует
  «16-й км МКАД, 50 метров от внешней стороны», мобильная (2225:107634) — то же
  плюс «, ул. Энергетиков, д. 22, корп. 3». Место одно, текста два. Сейчас
  везде стоит короткий вариант; нужен ответ, какой верный.
- **Розничный режим Контактов собран по живому сайту, а не по макету.**
  Розничного состояния макет не рисует ни на 1440, ни на 360 (в обоих кадрах
  активен «Опт»), поэтому и поведение, и данные взяты со страницы
  mebel.com/contacts (выгружено 23.08.2026): область → город → список
  магазинов. В фикстуре лежит Московская область целиком (Москва и 29 городов,
  около семидесяти адресов); остальные тридцать один регион из селектора сайта
  придут с бэка. Данные стоит сверить с базой — они вычитаны со страницы, а не
  выгружены.
- **Розничные адреса выгружены со страницы-источника, а не из базы.** Все
  тридцать два региона, 118 городов и 210 магазинов сняты со страницы
  mebel.com/contacts 24.08.2026 обходом селекторов; координаты взяты оттуда же
  (в разметке карточки они лежат в `data-latitude` / `data-longitude`). Данные
  стоит сверить с базой и обновлять оттуда же, а не руками. У региона без
  магазинов панель показывает «Ничего не найдено» — копия взята у пустой
  выдачи каталога, своей макет не рисует.
- **Оптовый склад в макете и на сайте один — московский.** Селектор города в
  опте повторяет сайт: пять городов, карточка склада одна и та же. Нужны
  адреса складов в Санкт-Петербурге, Нижнем Новгороде, Ростове-на-Дону и
  Рязани — они встают в `WHOLESALE_CITIES` без правок разметки.
- **Селекторов города и области макет не рисует.** Город — это подпись в шапке
  панели («Москва» с пином, 1456:56787), она и стала кнопкой; область — вторая
  строка под ней. Оформление наше, поведение — с сайта.
- **Снимков склада в макете три, и они ровно во всю ширину рельса.** 438 × 3 +
  24 × 2 = 1362 при 1360, то есть мотать нечего, хотя стрелки дизайнер
  нарисовал. Чтобы слайдер работал, в прототипе те же три снимка идут дважды;
  нужен настоящий набор фотографий.
- **Клик по фамилии сотрудника никуда не ведёт.** Имя набрано пунктирной
  ссылкой, значит это кнопка, — но что она открывает (карточку, почту,
  телефон), макет не говорит.
- **Координат склада в макете нет.** Карте нужна точка, поставлена МКАД у
  Котельников по адресу из панели. Заменить на настоящие.
- **Пятым табом на «Для интернет-магазинов» стоит «Где купить».** Четыре
  первых — форматы выгрузки (JSON, XML, YML, CSV), пятый не переопределён от
  таб-бара PDP и на странице про фиды смысла не имеет. Отрисован — его рисуют
  оба кадра, — но поведения у него нет: форматом он не является, а блока
  «Где купить» на странице нет. Нужен ответ, убирать совсем или это что-то
  значит. На 360 (2209:104388) таких табов **два**: на 1440 дизайнер один из
  них скрыл, на мобильном — нет; отрисован один.
- **«Скачать пример разбора выгрузки JSON» окрашен наполовину.** В обоих
  кадрах (1167:74245, 2209:104275) коралловым «Link M dotted» набраны только
  первые два слова, хвост — обычный #292929. У соседней строки «Скачать архив
  изображений» коралловая вся, и она же настоящая ссылка. Отрисованы обе
  целиком коралловым пунктиром; в макете поправить.
- **Состояний XML, YML и CSV в макете нет** — нарисован только JSON. Смена
  формата сейчас переписывает подписи и адреса, содержимое таблицы одно на все
  четыре. Нужны кадры или подтверждение, что таблица общая.
- **Чередование заливки в «Описании формата» макет ведёт двумя разными
  способами.** В блоке «Элементы category» (1167:74289) строка с вложенной
  таблицей считается одной: метка серая, подстроки продолжают от неё, а
  следующая строка снова белая — колонка идентификаторов идёт строго через
  одну. В блоке 1167:74575 после такой же строки счёт продолжается по
  подстрокам, и в левой колонке подряд оказываются две белых. Свёрстано по
  первому (строго через одну, как и просил клиент); во втором блоке заливка
  правых ячеек с какого-то места разойдётся с макетом.
- **Последний адрес выгрузки в макете перенесён на две строки.** У строки
  «Рязань» ячейка ссылки сужена вручную до 154 (2036:159026) при 295 у
  соседних, отчего адрес ложится в две строки и вылезает из своих 44. У нас
  колонка одна на все пять строк и адрес в одну строку. Уточнить, нужен ли
  перенос.
- **Мобильная таблица «Описание формата» выше макета** — 7300 против 5170 при
  тех же колонках и кегле 12/16: описания переносятся на большее число строк,
  чем в Figma. Проверить на реальном тексте, когда он придёт с бэка.
- **«Работа с претензиями» на 360 разбита на два текстовых блока.** В секции
  2241:162290 обе строки-ссылки нарисованы отдельными `title-text-page` (12 +
  20 каждая), тогда как во всех остальных секциях абзацы идут одним блоком и
  без отбивок между ними (базовые линии ровно через 20). Свёрстано общим
  правилом, поэтому секция на 360 на 12 короче кадра.
- **Адреса ссылок на «Как с нами работать?» в макете не проставлены.** Восемь
  ссылок в тексте (конструктор, Excel, пакет документов, правила оформления
  документов, инструкция, регламент, бланк рекламации, сертификаты) стоят с
  `href="#"`. Нужны цели.
- **Ссылки на сами документы в «Сертификатах» не проставлены** — девятнадцать
  файлов стоят с `href="#"`, у всех одна дата «29.05.2026г.» из мастера.
  Нужны адреса и настоящие даты.
- **Поле поиска на «Схемах сборки» нарисовано двух ширин.** В свёрнутом кадре
  (2241:163429) это компонент `search` 481×44, в развёрнутом (1463:63550) —
  собранная руками форма 862×44 во всю колонку, как на «Сертификатах» и
  «Технической информации». Оставлено 481 (это состояние по умолчанию и
  единственный на страницах инстанс самого компонента); уточнить.
- **Лупа в поле «Поиск по документам» белая на белом.** На всех трёх страницах
  с деревом инстанс иконки взят в варианте `color=light` (43:1272), поэтому в
  рендере Figma её просто не видно. Отрисована тёмной; подтвердить, что это
  описка в макете.
- **Поиск по документам ищет по странице.** Прототипа у поля в макете нет, и
  по решению клиента он клиентский: дерево уже загружено целиком, запрос
  прячет несовпавшие строки и раскрывает ветки, в которых что-то нашлось.
  Совпадение имени ветки показывает её целиком. **Пустого состояния макет не
  рисует** — при отсутствии совпадений дерево остаётся пустым, без надписи;
  нужна копия, если она нужна.
- **Поля «Поиск по документам» ниже md нет вовсе** — мобильный фрейм
  начинается сразу с дерева документов.
- **Нижние 12 у веток дерева расставлены вручную.** Правило, которое сходится
  почти везде: у ветки 12 снизу, но у последней ветки вложенного списка их нет
  (1167:60903, 1463:63640 — 40 против 52 у соседей). Из него выбиваются два
  места на «Схемах сборки»: у «Комплектующих для кухни» — последней ветки
  списка «Кухни» — эти 12 есть (1463:63655), а у «Каркасов», которые не
  последние, их нет (1463:63561 = 704 = 40 + 664). Свёрстано по правилу,
  отчего «Комплектующие» стоят на 12 ниже кадра; итоговая высота дерева при
  этом совпадает (1036). Уточнить.
- **Дерево документов по умолчанию свёрнуто целиком** — решение клиента. В
  кадрах «Сертификатов» (1463:60818) и «Технической информации» (1463:60871)
  первая группа раскрыта; «Схемы сборки» и так нарисованы свёрнутыми.
- **Свёрнутая группа документов нарисована двух высот.** На «Схемах сборки» в
  свёрнутом кадре (1463:60735) она 64, а в развёрнутом кадре той же страницы
  (1463:63542) и на «Сертификатах» — 52. Взяли 52: два кадра из трёх и один
  общий компонент. Уточнить.
- **Схемы сборки: 92 документа без адресов** — все с `href="#"`.
- **У «Технической информации» нет мобильного фрейма.** Ниже md страница
  построена по правилам соседних страниц с деревом («Сертификаты», «Схемы
  сборки»): заголовок и поле поиска скрыты, имя несёт селектор в шапке
  раздела, дальше сразу дерево. Решение наше, не макета.
- **В свёрнутом кадре «Схем сборки» списки пустые** — содержимое нарисовано
  только в развёрнутом. Собрано оттуда, состояние по умолчанию свёрнутое.
- **Значения фильтров «Каталога декоров» знает только бэк.** Механика собрана
  целиком (воронка с бейджем, счётчики у пилюль, чипсы выбранного, «Очистить
  все», шторка и шов `applyDecorFilters()`), но шторки макет для этой страницы
  не рисует: названия групп взяты с пилюль, а значения — только те два, что
  названы в кадре выбранных параметров (1488:69674): «ДСП» у Материала и
  «Светлые» у Цвета. **У «Структуры» список пуст** — придумывать значения не
  стали. Нужны полные списки по трём группам.
- **Сетки декоров и 3D-моделей швом не фильтруются.** Признаков (структура/материал/цвет) у
  карточек нет ни в макете, ни в фикстурах, поэтому шов пишет параметры в
  адрес и обновляет панель, а состав выдачи оставляет как есть — он придёт с
  сервера. По той же причине «Показать N» показывает всё (48 и 30).
- **Порядок чипсов выбранного.** В кадре они идут «Светлые», «ДСП» (то есть
  цвет раньше материала), у нас — в порядке пилюль: Структура, Материал,
  Цвет. Если важен порядок выбора, скажите.
- **Что прячется за «еще ···» в ряду сегментов, макет не называет.** Подписей
  у него ровно шесть, и седьмым чипсом стоит «еще». По клику ряд дополняется
  рубриками показанных товаров, которых в нём ещё нет (из данных карточек), и
  дальше прокручивается вбок одной строкой. Нужен настоящий список рубрик.
- **Рубрики новостей без своих кадров.** Нарисован только «Все новости»;
  что показывают «Новинка», «График работы» и остальные пять, макет не
  говорит. Выбор пока только переставляет активную рубрику.
- **Кнопка постранички на «Новостях» считает новости, а не товары.** В макете
  там подпись мастера — «Показать еще 40 товаров из 123» (инстанс её не
  переопределяет), на странице новостей это ошибка копии; отрисовано
  «новостей». Числа (40 и 123) остались филлером макета.
- **Ссылки в новостях ведут в никуда** — у всех двадцати `href="#"`.
- **Каталог 3D-моделей: куда он ведёт из меню?** Фрейм 2338:254259 лежит в
  секции B2b additional и назван «Каталог (вариант)», но левого меню раздела у
  него нет, а в самом меню пункта под него не видно. Ближайший кандидат —
  «Каталог мебели», который сейчас заглушка.
- **Шторка «Каталога 3D-моделей» — каталожная.** Своей кадр не рисует, но
  пилюли в панели те же шесть (882:89341), и группы каталожной шторки
  совпадают с ними один в один, поэтому подключена она. Ряда чипсов у страницы
  в кадре нет: занятые группы показывают счётчики на пилюлях и бейдж воронки.
  Если для 3D-моделей нужен свой набор значений — сказать.
- **Рельс «Популярные товары» на главной.** В кадре 644:44576 у него нет
  кнопки «В раздел» (у двух соседних рельсов кнопки есть), а описание читается
  «Покупая товары для кухонь вместе с кухнями VIVAT, вы получаете самые
  выгодные цены!» — у нас стоит копия каталожного «Подберите полезные
  товары…». На каталоге и 3D-моделях кнопку убрали по их кадрам; главную не
  трогали — там расходится и порядок рельсов, это отдельный проход.
- **Мобильный рельс выше кадра на 74.** На каталоге и «3D-моделях» блок
  `other-row` в кадре 444, у нас 498 плюс полоса прокрутки 32. Разница в общей
  карусели (высота карточки на 360 и индикатор), а не в странице.
- **Все тридцать карточек 3D одинаковые** — один заголовок на всех, это филлер.
  У двух из них нет и рендера: картинки нет ни в инстансе, ни в мастере.
- **Рендеры 3D уменьшены до размера карточки.** Из экспорта они приходят
  полноразмерными PNG на 32 МБ; в репозиторий положены JPEG 644px на 492 КБ.
  Если нужны исходники — брать из VIVAT_SOURCES.
- **Фильтры на «Каталоге 3D-моделей» не подключены** — шторки для этого фрейма
  макет не рисует.
- **Сегменты «Опт / Розница» ничего не переключают** — прототипа на них нет ни
  на 1440, ни на 360. Состояние есть, поведения нет.
- **Раскрытая карточка магазина/дилера (953:55651) несёт одни и те же
  Консультация/Самовывоз-часы, email и сайт у всех 10 точек** —
  `mebel@dveribravo.ru` / `mebelbravo.ru` / расписание из карточки. Это
  единственный реальный контент в файле: сама раскрытая карточка нигде на
  странице не заинстанцирована, только мастер компонента с плейсхолдером
  стороннего дилера. Нужны настоящие email/сайт/расписание на каждую точку —
  вопрос клиенту, откуда их брать. Телефон и адрес — свои, настоящие.
  «Проложить маршрут» и соцссылки — `span`, не подключены (в макете нет
  прототипа).
- **Крошки каталога на мобиле, вероятно, тоже с неверным паддингом.**
  `get_design_context` на 1997:267658 даёт `py-[2px]` (буквально 2px, не
  Tailwind `py-2`=8px) при фиксированной высоте строки 40 и центровке 24px
  ряда внутри неё — модель не «паддинг + контент», а «фикс-высота + слот по
  центру», как и у мобильного `title-block`. У нас `max-md:py-2` (8px) без
  этой фиксированной высоты. Не трогал — нашёл только при сверке десктопа,
  на мобиле никто не жаловался и переносить модель наспех рискованно;
  стоит проверить отдельным проходом.
