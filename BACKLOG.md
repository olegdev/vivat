# Backlog

Only things that are actually open. Delete lines as they land.

## Known gaps

- Product cards show the same image three times — the gallery and its dots are
  real, the data isn't. Needs per-product image sets.
- Card data across all sections is mock (`src/pages/customer/main.js`). Counts
  were padded to ten to match the mobile 2×5 layout; prices and titles are
  invented.
- Every CTA is `href="#"`. No other page exists to link to.
- Hero slides 2 and 3 are mock — only slide 1 exists in Figma.
- Yandex Maps key is inlined as a fallback in `main.js`. Move to
  `VITE_YANDEX_MAPS_KEY` before this is public.
- PDP's «Состав» tab has no Figma frame — `partials/pdp-specs.html` holds
  placeholder rows there. Replace when the design lands.

## Дилерский раздел — ждём ответа дизайнера

Полная карта b2b-секций и все вопросы: `docs/FIGMA-MAP.md`. Здесь только то,
что уже уперлось в вёрстку.

- **Дилерская главная на 360 собрана без своего фрейма.** 360-фрейма у
  882:107882 по-прежнему нет. Шапка, нижняя навигация и выбор прайс-листа взяты
  из дилерского каталога 2225:160540 — это макет. А вот **новости рельсом,
  бургер-меню и дилерские ветки мобильного подвала** не нарисованы нигде и
  выведены нами: новости — 320-е карточки, как у мобильных «Акций», и две
  кнопки друг под другом (в ряд не влезают); меню — навигация дилерской шапки
  на 1440 («Каталог», «Конструктор», «Контакты») плюс ссылки дилерской полоски
  («Для бизнеса», «Мой кабинет», «Выход»); подвал — те же две ветки, что у
  десктопного. Показать дизайнеру.
- **Из дилерского бургер-меню убраны «Где купить», «Компания» и «Полезная
  информация».** Они достались от покупательского набора (1997:255059), а в
  дилерской навигации их нет: шапка на 1440 несёт «Конструктор» и «Контакты».
  При этом «Полезная информация» и «Компания» — реальные ветки дилерского
  подвала, так что вопрос открыт: должны ли они быть и в меню.
- **На 360 дилерские фреймы подставляют покупательский подвал.** В каталоге
  2225:160540 инстанс подвала — вариант `user=Default` (1821:327818), тогда как
  на дилерском 1440 стоит `user=Dealer` (380:49273). Мы рендерим дилерский на
  обеих ширинах: иначе мобильная версия теряет «Личный кабинет» и вторую ветку
  ссылок, которые на десктопе есть. Похоже на тот же пропуск, что и с крошками —
  360-фреймы дилера собраны из покупательских частей.
- **Дилерский каталог на 360 идёт без хлебных крошек и заголовка.** Во фрейме
  2225:160540 за шапкой сразу следует панель настроек: ни `breadcrumbs`, ни
  `title-block` там нет, хотя на дилерском 1440 (953:121911) и на
  покупательском 360 (1997:267656) они есть. Собрано по макету — ниже `md`
  блок скрыт. Спросить дизайнера, это решение или пропуск.
- **«Только модули» — тумблер без прототипа.** Дилерская панель настроек
  (`catalog-settings type=dealer`, 882:89379 на 1440 и 1806:238263 на 360)
  добавляет к покупательской один тумблер, и что он делает, макет не говорит.
  По решению заказчика это обычный фильтр: применяется сразу и уходит в URL
  полем `modules_only`. Серверного поля у товара нет — в фикстурах стоит
  заглушка `isModule` через один (`data/catalog.js`). Нужно поле или правило.
- **Дилерская панель фильтров на 1440 — отцепленная копия.** 953:121956 рисует
  пять групп вместо шести (нет «Формы кухни»), и у первой потерян заголовок —
  рендерится текст мастера «Форма кухни» вместо «Коллекции». Покупательский
  фрейм 759:69866 держит ту же панель инстансом компонента, поэтому
  переиспользуем его. Поправить в исходнике.
- **Инстанс дилерской панели настроек не несёт ни одного переопределения
  текста** (то же и у покупательской 882:89607), поэтому `npm run audit` по
  ней сравнивает страницу с филлером мастера «Текст» и всегда даёт расхождение.
  Проверять эту панель приходится по размерам боксов (`fig.mjs inst`), а не по
  копии. Дизайнеру: проставить тексты в инстансах.
- **Откуда берётся «Рекомендованная цена».** Пересчёт работает, но множителя в
  дизайне нет: в `data/dealer-home.js` стоит заглушка `RRP_FACTOR = 2`. Нужно
  поле у товара или правило.
- **Раскрытая «Своя наценка» на 1440 не нарисована.** Вариант
  `dropdown-header desktop/open` (1299:49518) рисует три строки по 44 и всё.
  Поле ввода и «Применить» перенесены с мобильной шторки 2225:164865.
- **Опечатка в макете: «Рекомендованая цена»** (одна «н») — рендерим
  «Рекомендованная». Поправить в исходнике, тогда `npm run audit` по строкам
  списка снова сойдётся.
- **Подпись «Минимальная наценка 50%»** у дизайнера 11/14 — размера, которого
  нет в шкале. Взят ближайший токен `body-xs` (12/16).
- **Отступ полоски.** Инстанс говорит `padH=12`, мастер — 24, дочерний фрейм
  стоит на x=24. Взято 24: при 12 тумблер залезает в скругление 32px.
- **Тумблер «Показывать цену»** прототипа не имеет; поведение задал заказчик:
  это выключатель применения выбранного прайс-листа. Выключен — цены оптовые,
  включён — выбранный в списке режим. Ничего не скрывает. Открытым остаётся
  одно: выбор режима при выключенном тумблере сейчас **не** включает его
  автоматически — цены не меняются, пока тумблер не вернут. Подтвердить.
- **Дилерский PDP на 360 собран без своего фрейма.** У 953:122180 и обеих его
  панелей (1686:59341, 1686:59383) 360-фреймов нет. Тело ниже `md` взято с
  покупательского мобильного PDP (1997:305719 и его панели), обвязка — из
  дилерского каталога 2225:160540. Выведено нами три вещи: **мобильная сводка в
  дилерском виде** (варианта `device=mobile, Customer=b2b, Type=PDP` не
  существует — перенесли на неё десктопную дельту b2b: цена одной строкой, без
  «от», без старой цены, без бейджа скидки и без строки notice), **подпись
  мобильной кнопки заказа** («Оформить • 43 335₽» вместо «Оформить • от 43 335₽»)
  и **серая полоса под отзывами** на 360. Показать дизайнеру.
- **Рельс «Вся коллекция Фьюжн» на дилерском PDP нарисован с «от».** Во фрейме
  1686:63229 карточки идут как «от 450 010₽» — покупательский инстанс,
  скопированный вместе со страницей. При этом дилерский каталог (1686:158738) и
  сводка этого же PDP «от» не имеют. По решению заказчика от 12.08.2026 рендерим
  без «от» на всей дилерской половине. Поправить в исходнике.
- **Нижняя панель с ценой у дилера не нарисована.** У покупателя это отдельные
  фреймы `sticky price` 1884:366246 и `DPD` 2027:83602; у дилерского PDP их нет.
  Переиспользованы покупательские (`partials/sticky-price.html`) — без них на
  360 у страницы вообще не было бы кнопки заказа, мобильная сводка её не несёт.
  Цена в них приведена к дилерскому виду: без «от» и без старой цены.
- **Пересчёт цены товара прайс-листом прототипа не имеет.** На дилерском PDP
  цена в сводке и в нижней панели теперь подчиняется выбранному прайс-листу так
  же, как цены карточек (решение заказчика от 12.08.2026). В макете связи между
  полоской и ценой товара не нарисовано ни на одном фрейме.
- **Соцсети в подвале**: на десктопе шесть (с MAX), на мобильном пять.
  Расхождение досталось от покупательских фреймов, у дилерских своего 360 нет.
- **Блок «Производство»**: у дилерского фрейма затемнение `#111111/0.8`, у нас
  `overlay-middle` (0.6) — как было на покупательской. Уточнить, какое верно.
- **Полоса alert** на покупательской главной — старая инлайновая копия без
  иконки и крестика, предшествует `partials/alert.html`. Свести к партиалу,
  когда дизайнер подтвердит, что иконка и × нужны и там.

## Seams to wire (form + request seam pattern — see SOLUTIONS.md)

Places that will be a server round-trip in the Blade build. Done so far: catalog
filter drawer, popular-carousel tabs, site search (overlay + suggest),
add-to-cart, stores filter.

The JS-string structure debt is unwound: every repeated unit is now a clean HTML
`<template>` in its partial that the component clones (product-card, catalog-menu,
mobile-menu, stores). What stays in JS is only the mock *data* arrays
(`categories`, `stores`, the product lists) — in Blade those come from the model
and are printed into the same templates; nothing structural remains to convert.

- Search suggest is a seam in one function, `searchSuggest()` in
  components/search.js — it returns `{ hints, chips, items }`, exactly the shape
  a `/search/suggest?q=…` response should have. Its mock corpus is
  `src/data/search.js`; both go away when the endpoint lands. The overlay's own
  `<form action="/search" name="q">` already submits the real query.
- The designer re-cut `cards-other size=s` (Figma 632:27760): it now carries a
  swatch + comments row above the category tag, 322×410 instead of 386. The
  search card (`data-pcard-search`) is built to the new shape; the PDP's
  "Добавьте в корзину" rail still uses the old `data-pcard-other-s` template. No
  visible difference today — that rail's fixtures carry no swatches — but the
  two templates should be folded into one when someone next touches the PDP.

## Хвосты

- **`docs/PORTING.md` не знает про дилерскую половину.** Список готовых
  страниц, таблицы партиалов, компонентов и фикстур перечисляют только
  покупательские файлы — дилерских главной, каталога, PDP и заказа там нет ни
  строки. Долг накопился за четыре страницы, чинить его надо целиком, а не по
  одной странице за раз.
- **«Изменить выбор» в алерте шага 2 у покупателя.** Инстанс 953:115338 эту
  ссылку прячет — во фрейме остаются только галочка и текст «Вы выбрали …».
  На странице она есть. Проверить по мобильному фрейму 2082:145162 и либо
  убрать, либо записать, откуда взялась.

## Дилерский заказ — вопросы по макету

- **Список способов оплаты не нарисован нигде.** Селект «Оплата» несёт только
  плейсхолдер, и мобильной шторки под него нет ни в одном фрейме. Собран
  рабочий `<select>` с одним пунктом; варианты нужны от заказчика.
- **Надбавки за расстояние и этаж — без правила.** Оба бейджа во фрейме
  1415:67654 читают «+222₽», хотя поля разные (12 км и 8 этаж), а стоимость
  доставки — константа 3414₽. Ничего не считаем: цифры лежат в
  `data/dealer-order.js`, пересчёт — шов `applyDelivery()`. Нужна формула.
- **«Удалить» в шапке корзины дилерский инстанс не переопределяет** — рендерит
  мастерскую заглушку «56 моделей ✕». Взято «Удалить», как в покупательском
  фрейме 943:34134. Подтвердить.
- **Два мобильных фрейма расходятся по кнопке в сводке.** В 2225:202954 у
  сводки есть блок кнопок, в полном 2225:167283 его нет. Собрано по полному:
  сводка без кнопок, CTA — всегда видимая липкая полоса. Проверить.
- **«Не выбрано» против «Не выбрана».** 1209:95219 рисует «Не выбрано»,
  1415:67609 — «Не выбрана» в том же селекте. Взято «Не выбрано» — по основному
  фрейму; в сводке — «не выбран» («Способ оплаты»), как во фрейме. Три формы
  одного значения в одном экране; нужна одна.
- **На 360 у степпера нет состояния «корзина».** Все 360-карточки заказа —
  и дилерские (2225:167289), и покупательские (2029:156652) — рисуют минус
  рядом с единицей, тогда как на 1440 единица даёт корзину (943:79876).
  Собрано по макету: ниже `md` корзины нет, минус на единице ничего не делает.
  Значит, **удалить строку с телефона нечем** — ни свайпа, ни крестика в
  макете нет, а «Очистить корзину» есть только на 1440. Нужен ответ.
- **«Итого» есть на 360 и нет на 1440.** 953:149252 такой строки не рисует,
  2029:158160 рисует. Собрано по фреймам; похоже на пропуск в десктопном.
- **Кнопка липкой полосы на 360 нарисована с «от»** («Сформировать заказ • от
  43 335₽») — копия покупательского инстанса. Выведено без «от», как все
  дилерские цены (то же решение, что на дилерском PDP).
- **Экрана подтверждения в дилерской секции нет** — берём покупательский
  оверлей 2241:158412 / 2241:158297 (мобильный дилерский 2238:157458 — он же).

## Not started

Покупательские страницы (главная, каталог, акции, PDP, заказ) и дилерские
(главная, каталог, PDP, заказ) свёрстаны. Осталось — карта и node-id в
`docs/FIGMA-MAP.md`:

- **Секция `B2b additional`** — двенадцать контентных страниц по одному
  шаблону и четыре модальных окна (заявка, вход, подписка, письмо директору).
  Десктопные панели окон в снапшоте отсутствуют — смотреть в живой Figma.
